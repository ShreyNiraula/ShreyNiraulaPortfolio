node_modules
*/node_modules

# Build and cache output
dist
.astro

# Local environment and editor metadata
.venv
.env
.env.local
.idea

# OS artifacts
.DS_Store

# Local-only (not used by the deployed site, never push)
# NOTE: anchored to repo root so it does NOT match public/img/skills/
/skills/
/CLAUDE.md
