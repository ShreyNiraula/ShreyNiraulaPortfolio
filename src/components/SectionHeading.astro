---
// Section heading: bold uppercase Archivo title + mono "// kicker".
interface Props {
  title: string;
  kicker: string;
}
const { title, kicker } = Astro.props;
---

<div class="sec-head reveal">
  <h2>{title}</h2><span class="kick"><span class="slash">//</span> {kicker}</span>
</div>
